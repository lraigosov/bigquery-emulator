# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/
dist

# Locally built emulator binary (produced by `make emulator/build`)
/bigquery-emulator

# Local output of the e2e conformance suite
/test/e2e/.out/

# Maven build output from the Java e2e client
/test/e2e/clients/java/target/
/test/e2e/clients/java/dependency-reduced-pom.xml

# Local reproduction scripts and logs
/repro/

# Local development log (not for version control)
devlog.md
